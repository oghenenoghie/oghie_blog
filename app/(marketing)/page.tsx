// Route superseded by app/page.tsx (non-grouped route takes precedence in Next.js).
// Content and layout live there to avoid the duplicate-route conflict.
export default function MarketingPage() {
  return null;
}
